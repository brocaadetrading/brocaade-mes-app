{
  "version": 2,
  "buildCommand": "mkdir -p public && cp frontend.html public/index.html && cp frontend.html public/frontend.html",
  "outputDirectory": "public",
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "/server.js"
    },
    {
      "src": "/(.*)",
      "dest": "/index.html"
    }
  ]
}
